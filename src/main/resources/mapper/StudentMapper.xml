<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//MyBatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.today.App.mapper.StudentMapper">

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT *
        FROM student
    </select>
    <select id="selectOne" parameterType="Long" resultMap="BaseResultMap">
        SELECT
        *
        FROM student
        WHERE id = #{id}
    </select>
    <insert id="insert" parameterType="com.today.App.model.Student" >
        INSERT INTO
            student
            (name,sex,age)
        VALUES
            (#{name}, #{sex}, #{age})
    </insert>
    <resultMap id="BaseResultMap" type="com.today.App.model.Student">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="sex" property="sex" javaType="INTEGER"/>
        <result column="age" property="age" jdbcType="INTEGER"/>
    </resultMap>

</mapper>